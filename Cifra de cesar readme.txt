1-Primeiro importamos o arquivo "string" para ter acesso a sequencia de caracteres

2-colocamos a função criptografar para começar o processo de criptografia

3-Inserimos as mensagens que irão aparecer quando o código for processado com print e input

4- inserimos a variavel "criptgrafada_mensagem" 

5 - utilizamos uma estrutura de repetição com o for dentro da variavel mensagem que iriamos inserir ao rodar o código

6- em seguida usamos um estrutura de condição com o if para ordenar o uso do alfabeto na criptografia, inserido junto tambem a chave de criptografia

7- e por final da primeira parte ativamos a função "criptografia"

8- Assim demos inicio a segunda parte e inserimos a função decrypt para decriptografar a palavra

9- inserindo tambem o código com a chave de decriptografia que deve ser a mesma para criptografia

10- Utilizamos a estrutura de repetição for novamente para seguir o mesmo conceito de ordenar o embaralhamento da palavra por meio do alfabeto

11- no final damos um print final nas para aparecer o código e ativamos a função.
 